# --- API service ---
fastapi
uvicorn[standard]

# --- Core runtime (unificado EDA + DEP + MCE) ---
pandas>=2.0,<3.0
numpy>=1.23,<2.0
scikit-learn>=1.3,<2.0
scipy>=1.10,<2.0
joblib>=1.2
pyyaml>=6.0
fsspec>=2023.4.0

# --- Configuración ---
hydra-core>=1.3
omegaconf>=2.3

# --- Modelos / algoritmos ---
xgboost>=1.7

# --- Visualización (útil pero no estrictamente necesario para el API) ---
matplotlib>=3.7,<4.0
seaborn>=0.13,<0.14

# --- Opcional, solo si de verdad necesitas notebooks dentro del contenedor ---
# ipykernel>=6.0,<7.0
