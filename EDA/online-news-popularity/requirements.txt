# ---------- core runtime ----------
pandas>=1.5
numpy>=1.22
pyyaml>=6.0
fsspec>=2023.4.0       # abstraction layer for local/remote file systems

# ---------- configuration ----------
hydra-core>=1.3
omegaconf>=2.3

# ---------- optional visualisation (not required for pipeline) ----------
matplotlib>=3.7
seaborn>=0.13

# ---------- development / CI ----------
pytest>=7.4
black>=23.9
ruff>=0.1.0
mypy>=1.5
build>=0.10            # python -m build
twine>=4.0             # publishing to PyPI (optional)

# ---------- cloud storage back-ends (install only what you need) ----------
# s3fs>=2023.4.0       # AWS S3
# gcsfs>=2023.4.0      # Google Cloud Storage
# adlfs>=2023.4.0      # Azure Data Lake / Blob